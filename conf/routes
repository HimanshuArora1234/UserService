# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

POST    /v1/page                    controllers.PageViewController.pageView
GET     /v1/user/:userid            controllers.UserProfileController.getUserProfile(userid: String)
DELETE  /v1/user/:userid            controllers.PageViewController.deletePageViews(userid: String)
